apply plugin: 'java'

project.buildDir = syslogBuildDir + '/kafka'

repositories {
  maven {
    url "http://build.syslog-ng.balabit:8080/repository/syslog-ng-pe/"
    credentials {
      username "syslog-ng"
      password "titkos12"
    }
  }
  flatDir {
    dirs syslogDepsDir
  }
}

dependencies {
  compile name: 'SyslogNg'
  compile 'org.syslog-ng.kafka:kafka-deps:5.4'
  testCompile 'junit:junit:4.12'
  testCompile 'org.hamcrest:hamcrest-core:1.3'
  runtime 'org.syslog-ng.kafka:kafka-deps:5.4'
}
